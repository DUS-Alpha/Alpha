using UnityEngine;

public class TestSkw : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Demerge();
    }

    // Update is called once per frame
    void Update()
    {
        
    }



    void Demerge()
    {
        print("좋아!");
    }
}
